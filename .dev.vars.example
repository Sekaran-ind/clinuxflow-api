# Copy this file to .dev.vars (gitignored) for local `wrangler dev`.

# Overrides wrangler.toml's [vars] ENVIRONMENT = "production" — keeps Designer's dev-testing
# test-scribe harness working locally.
ENVIRONMENT=development

# Shared secret clinux-frontend sends as X-Service-Key — must match the frontend's
# VITE_SERVICE_KEY for local dev. Any value works locally; production uses a real random secret
# set via `wrangler secret put SERVICE_KEY`.
SERVICE_KEY=dev-local-shared-secret

# Signs/verifies per-account session JWTs (see src/lib/session.js). Any value works locally;
# production uses a real random secret set via `wrangler secret put JWT_SECRET`. Must differ
# from SERVICE_KEY — they gate different things.
JWT_SECRET=dev-local-jwt-secret

# Phase E video calling (see src/lib/realtime-client.js, POST /api/realtime/join). Leave unset
# to leave video calling disabled locally (the endpoint 501s cleanly) — fill these in with real
# values from a Cloudflare RealtimeKit app (dash.cloudflare.com -> Realtime -> Kit) once you have
# one, to test it for real.
# CF_REALTIME_ACCOUNT_ID=
# CF_REALTIME_APP_ID=
# CF_REALTIME_API_TOKEN=
# CF_REALTIME_PRESET_NAME=group_call_host
