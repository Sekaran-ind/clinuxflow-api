{
  "name": "clinuxflow-api",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "predeploy": "npm run build:kernel",
    "build:kernel": "npm run build:dictionary && npm run build:graphs-bundle && npm run build:system-forms",
    "build:dictionary": "node tools/dictionary-builder.js",
    "build:graphs-bundle": "node tools/build-graphs-bundle.js",
    "build:system-forms": "node tools/build-system-forms.js"
  },
  "dependencies": {
    "ajv": "^8.20.0",
    "ajv-errors": "^3.0.0",
    "hono": "^4.6.0",
    "js-yaml": "^5.2.0",
    "uuid": "^14.0.1"
  },
  "devDependencies": {
    "@smile-cdr/fhirts": "^2.4.1",
    "playwright": "^1.61.1",
    "ts-morph": "^28.0.0",
    "wrangler": "^4.0.0"
  }
}
